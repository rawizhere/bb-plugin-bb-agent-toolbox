{
  "name": "bb-plugin-bb-agent-toolbox",
  "version": "0.2.2",
  "type": "module",
  "license": "MIT",
  "engines": {
    "bb": ">=0.43",
    "bbPluginSdk": ">=0.4.84"
  },
  "bb": {
    "name": "BB Agent Toolbox",
    "description": "Give any coding agent — opencode, Prime Agent, Google Antigravity, Codex, Claude Code, and more — hands-on access to bb: read and edit workspace files, inspect and message other threads, wait for their results, answer their pending questions and approvals, delegate work by spawning threads, and see running terminals. All as tools the agent can call directly, whatever provider or model (incl. OpenRouter) it runs on.",
    "branding": {
      "icon": "./icons/bb-agent-toolbox.svg"
    },
    "server": "./server.ts"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "0.4.84",
    "@types/node": "^22.0.0",
    "better-sqlite3": "^13.0.3",
    "cron-parser": "^5.10.0",
    "hono": "^4.13.7",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "test": "bun test/smoke.mjs"
  }
}
